# spellchecker:ignore netrwhist sessionx

# Personal scratchpad — gitignored to reduce noise
/TODO.md

# Agent tooling state
/.claude/**

### Firmware ###
# Keep firmware images out of the repo: they are a) binary blobs and
# b) may contain credentials (a stock dump holds the device Wi-Fi SSID/password).
*.bin

### OpenSCAD ###
# Rendered / exported artifacts — regenerated from wb3s_jig.scad
*.stl
*.3mf
*.off
*.amf
*.csg
*.dxf
*.svg
*.png
# Dependency / echo files from `openscad -d` / `--export-format`
*.deps
*.echo

### Linux ###
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

### Vim ###
[._]*.s[a-v][a-z]
[._]*.sw[a-p]
Session.vim
Sessionx.vim
.netrwhist
tags
[._]*.un~

### Visual Studio Code ###
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.history/
*.vsix
